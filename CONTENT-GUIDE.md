# 中京教会 公式サイト 運用ガイド

## これは何の作業だったか

添付いただいた4つのファイル（index.html／style.css／script.js／content.js）は、
それぞれ**別の時期に作られた別バージョンの部品**で、互いに組み合わせても
動かない状態でした（例：script.jsが探しているHTMLの要素が、index.htmlには
存在しない、CSSがindex.htmlのクラス名と9割一致しない、など）。

今回、**実際に公開されているindex.htmlの構成を正としてすべて作り直し**、
1つの完全に動く状態にしました。

## 📁 最終的なファイル構成

```
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── content.js      ← ★本文データ（予定・牧師紹介・歴史など）はここを編集
│   └── script.js        ← 画面の動き（データ反映・メニュー・演出など）
├── admin/
│   └── schedule.html    ← ★今月の予定＋お知らせの更新専用ページ（GitHub不要・パスワードのみ）
├── functions/
│   └── api/
│       ├── contact.js       ← お問い合わせフォームの送信処理
│       ├── schedule.js       ← 管理画面が保存する予定データの読み書き処理
│       └── announcements.js  ← 管理画面が保存する「お知らせボード」データの読み書き処理
└── assets/
    ├── images/
    │   ├── hero/          （トップの背景・ようこそ写真）
    │   ├── pastor/         （牧師の写真）
    │   ├── facilities/     （施設紹介：礼拝堂・オルガン・母子室・バリアフリー）
    │   ├── history/        （歴史年表の写真）
    │   ├── church-school/  （教会学校の写真）
    │   └── instagram/      （Instagram未連携時の代替写真）
    └── audio/              （パイプオルガン試聴音源など）
```

## ⚠️ 写真について（重要）

いただいた写真（organ.jpg／pastor.jpg／01〜04.jpg／1898.jpg／sanctuary.jpg）を
仮置きしていますが、**AI生成または一般的なストック画像に見える**ものが含まれています
（実際の中京教会の外観写真とは建物の形状が異なるものがあります）。特に牧師の写真は、
実在の稲田勝之牧師ご本人の写真でない可能性が高いです。

**公開前に、実際の写真へ差し替えることを強くおすすめします。** 差し替え方法は
下記「② 写真を差し替える」の通り、同じファイル名で上書きするだけです。

## ✏️ 更新方法

### ① 今月の予定を更新する（GitHub不要・一番簡単）

`https://（サイトのドメイン）/admin/schedule.html` にアクセスし、パスワードを
入力するだけで使える更新専用ページです。

1. パスワードを入力して「開く」
2. 「＋ 日を追加する」で日付をカレンダーから選び、見出しと予定内容を入力
   - 曜日は日付から自動で計算されるので、入力の必要はありません
3. 「保存する」を押すと数分でサイトに反映されます

**事前準備（Kenさんが最初に1回だけ行う設定）**

1. Cloudflare Pagesの「設定」→「Functions」→「KV namespace bindings」で
   新しいKV namespaceを作成し、変数名 `SCHEDULE_KV` としてバインドする
2. 「設定」→「環境変数」で `ADMIN_PASSWORD` を追加し、更新画面で使う
   パスワードを決める
3. 更新する方には、管理画面のURLとパスワードだけを伝えればOKです

管理画面が未設定の間は、`js/content.js` 内の `services.schedule` に書かれた
バックアップの予定がそのまま表示されるので、サイトが空白になることはありません。

### ①-2 「教会からのお知らせ」ボードを更新する（台風による休会など）

同じ `admin/schedule.html` の中に、②として「教会からのお知らせ」の管理欄も
追加しました。**新しいCloudflareの設定は不要**です（①と同じKV・同じパス
ワードをそのまま使います）。

1. 管理画面を開く（①と同じパスワードでOK）
2. 「＋ お知らせを追加する」から、種類（⚠緊急＝赤／ℹ お知らせ＝青）・
   タイトル・本文を入力
   - 例：⚠緊急「台風〇号接近に伴う臨時休会のお知らせ」
3. 「お知らせを保存する」を押すと数分でサイトのトップに反映されます
4. 用件が済んだら、その項目を「この項目を削除」してから再度保存すると、
   サイトから自動的に消えます

お知らせが1件もない時は、サイトのトップには何も表示されません
（ヘッダー直下に何もない、いつも通りの状態になります）。

### ② 写真を差し替える

`assets/images/` 内の対応するフォルダに、**同じファイル名で上書き**するだけです。
HTMLもJavaScriptも一切触る必要はありません。

新しい写真を追加したい場合は、`js/content.js` の `SITE_MEDIA` 内のパスを
新しいファイル名に書き換えてください。

### ③ 予定以外のテキストを更新する（歴史・牧師紹介・献金口座など）

`js/content.js` を直接編集します。JavaScriptの記法（`{ }`や`,`）を保つ必要が
あるため、こちらは引き続きKenさんの担当をおすすめします。

### ④ 「初めての方へ」の重複整理について

「当日の流れ（flow）」と「よくある質問（faq）」で役割を分け、同じ内容が
2箇所で説明されないようにしています。

- **flow** … 教会に来てから礼拝が始まるまでの“手順”（受付→着席→開始→献金）
- **faq** … 来る前に気になる“不安・疑問”への回答（洗礼・服装・子連れ・バリアフリー・時間・駐車場）

今後この2つに情報を追加する際も、「手順の話か、不安への回答か」で書き分けると
重複を防げます。

### ⑤ お問い合わせフォーム

`functions/api/contact.js` がCloudflare Pages Functionsとして動作し、フォーム
送信内容をメールに変換して送ります。使う前に、ファイル内の `TO_EMAIL` /
`FROM_EMAIL`（またはCloudflareの環境変数）を実際の値に変更してください。
未設定・エラーの場合は自動的にメールソフトが開く `mailto:` にフォールバック
するので、公開直後でも壊れて見えません。

## 🚀 デプロイ

これまで通り、GitHubリポジトリにpush → Cloudflare Pagesで自動デプロイ、という
流れです。ビルドコマンドは不要（静的サイトのため）、公開ディレクトリはリポジトリの
ルートのままで問題ありません。

## 🔧 ローカルでの確認

`js/content.js` と `js/script.js` は `<script>` タグで直接読み込む方式にしたため、
`index.html` をダブルクリックして開くだけ（`file://`）でも正しく表示されます
（サーバーを立てる必要はありません）。ただし `/api/contact` と `/api/schedule` は
Cloudflare Pagesにデプロイした環境でのみ動作します。
